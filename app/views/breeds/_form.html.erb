<%= form_for(@breed) do |f| %>
  <% if @breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@breed.errors.count, "error") %> prohibited this breed from being saved:</h2>

      <ul>
      <% @breed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    
    <!--<%= f.label :breed_id, "Breed ID" %>-->
    <!--<%= f.text_field :breed_id %>-->
    
    <%= f.label :breed_name, "Breed Name" %>
    <%= f.text_field :breed_name %>
    
    <%= f.submit 'Submit', class: 'button' %>
  </div>
<% end %>
